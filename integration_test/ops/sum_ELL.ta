# RUN: comet-opt --convert-to-loops --convert-to-llvm %s &> sum_ELL.llvm
# RUN: export SPARSE_FILE_NAME0=%comet_integration_test_data_dir/test_rank2.mtx
# RUN: mlir-cpu-runner sum_ELL.llvm -O3 -e main -entry-point-result=void -shared-libs=%mlir_utility_library_dir/libmlir_runner_utils%shlibext,%comet_utility_library_dir/libcomet_runner_utils%shlibext | FileCheck %s

def main() {
	#IndexLabel Declarations
	IndexLabel [i] = [?];
	IndexLabel [j] = [?];           

	#Tensor Declarations
	Tensor<double> A([i, j], {ELL});

    #Tensor Readfile Operation 
	A[i, j] = comet_read(0);

	#Tensor Transpose
	var a = SUM(A[i, j]);
	print(a);
}

# Print the result for verification.
# CHECK: data = 
# CHECK-NEXT: 28.2,
